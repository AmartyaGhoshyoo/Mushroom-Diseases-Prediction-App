{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "lib": ["es6", "dom"], // Include "dom" in the lib array
    "jsx": "react" // Ensure JSX is properly set
  }
}
